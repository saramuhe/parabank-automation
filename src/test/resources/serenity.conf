serenity {
  project.name = "Reto Técnico Encora"
  take.screenshots = FOR_EACH_ACTION

  webdriver {
    driver = chrome
    chrome.driver = drivers.CustomChromeDriver
    firefox.driver = drivers.CustomFirefoxDriver
    edge.driver = drivers.CustomEdgeDriver

    maximize.browser.window = false
  }

  reports {
    show.step.details = true
    show.test.details = true
  }
}

cucumber {
  publish.enabled = false
  strict = true
}

logging {
  level = INFO
}
